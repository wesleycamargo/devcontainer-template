{
  "name": "AI Hermes Devbox",
  "dockerComposeFile": "docker-compose.yml",
  "service": "devcontainer",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  // Compose owns container lifecycle now (see docker-compose.yml); stop the
  // service on close instead of leaving it running detached.
  "shutdownAction": "stopCompose",
  "remoteUser": "hermes",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": "true",
      "username": "hermes",
      "upgradePackages": "false",
      "nonFreePackages": "true"
    },
    "ghcr.io/rchaganti/vsc-devcontainer-features/azurebicep:1": {
      "version": "latest"
    },
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  // Editor/development concerns only. Hermes' gateway and dashboard are
  // started by the image's entrypoint (HERMES_AUTOSTART_SERVICES in
  // docker-compose.yml), so no Dev Container startup hook is needed.
  "postCreateCommand": "cp /tmp/host-gitconfig ~/.gitconfig 2>/dev/null || true; cp /tmp/host-git-credentials ~/.git-credentials 2>/dev/null || true; chmod 600 ~/.git-credentials 2>/dev/null || true",
  "forwardPorts": [9119],
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "pwsh",
        "terminal.integrated.profiles.linux": {
          "pwsh": {
            "path": "pwsh"
          }
        },
        "terminal.integrated.fontFamily": "MesloLGM Nerd Font"
      },
      "extensions": [
        "ms-vscode.powershell",
        "anthropic.claude-code"
      ]
    }
  }
}

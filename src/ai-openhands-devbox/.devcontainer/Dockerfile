# AI OpenHands Devbox layers the OpenHands Agent Canvas companion service onto
# the published AI Devbox base. The Agent Canvas server itself runs as a
# separate Compose service; this image remains the VS Code development shell.
FROM ghcr.io/wesleycamargo/devcontainer-template/ai-devbox-image:latest

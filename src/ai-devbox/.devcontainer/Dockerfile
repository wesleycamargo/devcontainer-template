# Prebuilt base: PowerShell, Oh My Posh, Terminal-Icons, Node, and the Claude
# Code/Codex agent CLIs are already installed here (see
# Dockerfile.base, which .github/workflows/publish.yml builds and publishes
# as this image). Building from the image instead of Dockerfile.base means
# local rebuilds are fast instead of reinstalling everything from scratch.
FROM ghcr.io/wesleycamargo/devcontainer-template/ai-devbox-image:latest

# Add project-specific customizations below.

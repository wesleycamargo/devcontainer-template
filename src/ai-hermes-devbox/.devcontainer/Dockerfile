# Prebuilt base: everything from ai-devbox (PowerShell, Oh My Posh,
# Terminal-Icons, Node, the Claude Code/Codex agent CLIs) plus the Hermes
# Agent -- see Dockerfile.base, which .github/workflows/publish.yml builds and
# publishes as this image. Building from the image instead of Dockerfile.base
# means local rebuilds pull the prebuilt image instead of reinstalling
# everything from scratch.
FROM ghcr.io/wesleycamargo/devcontainer-template/ai-hermes-devbox-image:latest

# Add project-specific customizations below.
